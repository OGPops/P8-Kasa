function EstateTitle({ title, subtitle }) {
    return (
        <div className="title-estate">
            <h1>{title}</h1>
            <p>{subtitle}</p>
        </div>
    )
}

export default EstateTitle